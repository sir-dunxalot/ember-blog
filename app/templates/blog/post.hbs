{{#with model as |post|}}
  <header class="header">

    <h1 {{bind-attr class='mf.postTitle'}}>{{post.title}}</h1>

    {{partial 'blog/post-partials/published-date'}}
    {{partial 'blog/post-partials/tags'}}

  </header>

  <hr>

  <div {{bind-attr class='mf.postContent'}}>
    {{partial post.urlString}}
  </div>

  <hr>

{{/with}}

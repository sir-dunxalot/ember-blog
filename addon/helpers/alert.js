// TODO - change to component or block helper

import Em from 'ember';

export default Em.Handlebars.makeBoundHelper(function() {
// export default Em.Handlebars.makeBoundHelper(function(type, options) {
  // var icon = '<i class="icon-' + type + '"></i><span class="hidden">' + type + '</span>';
  // var open = '<dl class="alert-' + type + '"><dt>' + icon + '</dt><dd>';
  // var close = '</dd></dl>';

  // return Utils.wrapBuffer(open, close, options, this);
});
